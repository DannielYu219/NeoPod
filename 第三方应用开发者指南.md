# NeoPod 第三方应用开发者指南

## 概述

NeoPod 支持通过 HTML/CSS/JavaScript 开发第三方插件应用。这些应用可以在 NeoPod 的 Programs 页面中运行，提供丰富的交互功能。

## 文件结构

### 应用存放位置
- HTML 插件应用可放置在以下两个文件夹：
  - `Documents/dev/` - 开发者工作区，用于开发和测试
  - `Documents/program/` - 已安装的应用，从应用商店下载的应用存放在此
- 支持的文件格式：`.html` 和 `.htm`
- 支持递归加载子文件夹中的应用
- 应用会自动按文件名排序显示在 Programs 页面列表中

### 应用商店
- 通过应用商店（Store页面）可以下载和安装第三方应用
- 已安装的应用存放在 `Documents/program/` 文件夹
- 应用商店服务器地址可在设置页面配置

## App 包结构

### 标准应用包结构
```
my_app/
├── index.html      # 主入口文件（必需）
├── info.json       # 应用信息文件（推荐）
├── icon.png        # 应用图标（可选）
├── styles.css      # 样式文件（可选）
├── script.js       # 脚本文件（可选）
└── assets/         # 资源文件夹（可选）
    ├── images/
    └── sounds/
```

### info.json 配置文件
应用信息文件用于在 Programs 页面显示应用名称和详细信息：

```json
{
  "name": "我的应用",
  "version": "1.0.0",
  "description": "这是一个示例应用",
  "author": "开发者名称",
  "last_updated": "2026-02-19",
  "icon": "icon.png",
  "main_file": "index.html"
}
```

**字段说明：**
- `name`：应用显示名称（必需）
- `version`：版本号（必需）
- `description`：应用描述（可选）
- `author`：作者名称（可选）
- `last_updated`：最后更新日期（可选）
- `icon`：图标文件路径（可选）
- `main_file`：主入口文件，默认为 index.html（可选）

### 文件命名规范
- 使用英文或拼音命名，避免使用特殊字符
- 文件名应简洁明了，能够描述应用功能
- 例如：`calculator.html`、`todo_list.html`、`tetris_game.html`

## NeoPod API

### 关闭应用
应用必须提供明确的关闭按钮，使用以下代码实现：

```javascript
function closeWindow() {
    try {
        if (typeof NeoPod !== 'undefined' && NeoPod.close) {
            NeoPod.close();
        } else {
            window.close();
        }
    } catch (error) {
        console.log('无法关闭窗口:', error);
    }
}
```

### 文件操作 API
NeoPod 提供文件操作 API，允许应用读写应用文件夹中的文件：

```javascript
// 读取目录内容
const files = await NeoPod.readDir('dev/subfolder');
// 返回: [{ name: 'file.txt', isDirectory: false, size: 1024, modified: 1234567890 }, ...]

// 读取文件内容
const content = await NeoPod.readFile('dev/config.json');
// 返回: 文件内容字符串

// 写入文件
await NeoPod.writeFile('dev/data.txt', 'Hello World');

// 删除文件
await NeoPod.deleteFile('dev/old_file.txt');
```

**可访问的文件夹：**
- `dev` - 开发者工作区
- `program` - 已安装应用
- `music` - 音乐文件
- `video` - 视频文件
- `config` - 配置文件
- `cache` - 缓存文件

### API 错误处理
所有 API 调用都返回 Promise，建议使用 try-catch 处理错误：

```javascript
try {
    const content = await NeoPod.readFile('dev/data.txt');
    console.log('文件内容:', content);
} catch (error) {
    console.error('读取失败:', error.message);
}
```

## UI 开发规范

### 必需的界面元素

#### 1. 顶部导航栏
每个应用都应包含顶部导航栏，显示应用名称和关闭按钮：

```html
<div class="top-bar">
    <div class="app-title">应用名称</div>
    <button class="close-btn" onclick="closeWindow()" aria-label="关闭">✕</button>
</div>
```

```css
.top-bar {
    display: flex;
    justify-content: space-between;
    align-items: center;
    padding: 12px 16px;
    background: rgba(0, 0, 0, 0.3);
    backdrop-filter: blur(10px);
    border-bottom: 1px solid rgba(255, 255, 255, 0.1);
}

.app-title {
    font-size: 16px;
    font-weight: 500;
    color: #f57309;
}

.close-btn {
    width: 32px;
    height: 32px;
    border-radius: 50%;
    background: rgba(255, 255, 255, 0.1);
    border: none;
    color: #ffffff;
    font-size: 18px;
    cursor: pointer;
}
```

#### 2. 触屏优化
- 按钮最小点击区域：44x44 像素
- 使用足够大的字体（建议 16px 以上）
- 提供触觉反馈（通过视觉变化模拟）
- 避免依赖悬停状态（触屏设备无悬停）

### 推荐的视觉风格

```css
/* 主题色彩 */
:root {
    --accent: #f57309;           /* 强调色 */
    --background: #1a1a2e;       /* 背景色 */
    --surface: rgba(255, 255, 255, 0.05);  /* 表面色 */
    --text: #ffffff;             /* 文字色 */
    --text-secondary: rgba(255, 255, 255, 0.6);  /* 次要文字 */
}

/* 按钮样式 */
.btn {
    padding: 12px 24px;
    border: none;
    border-radius: 8px;
    font-size: 16px;
    font-weight: 500;
    cursor: pointer;
    background: var(--accent);
    color: var(--text);
    transition: all 0.2s ease;
}

.btn:active {
    transform: scale(0.95);
}
```

### 响应式布局
应用应适配不同屏幕尺寸：

```html
<meta name="viewport" content="width=device-width, initial-scale=1.0, user-scalable=no, viewport-fit=cover">
```

```css
body {
    height: 100vh;
    display: flex;
    flex-direction: column;
    overflow: hidden;
}

.container {
    flex: 1;
    overflow-y: auto;
    padding: 16px;
    padding-bottom: max(16px, env(safe-area-inset-bottom));
}
```

## 应用商店发布

### 打包应用
将应用文件夹打包为 ZIP 文件：

```bash
cd my_app
zip -r ../my_app.zip .
```

### 发布要求
1. 必须包含 `index.html` 主入口文件
2. 推荐包含 `info.json` 配置文件
3. 所有资源文件使用相对路径引用
4. ZIP 文件大小建议不超过 10MB

### 应用图标
- 推荐尺寸：128x128 像素
- 格式：PNG（支持透明背景）
- 文件大小：建议不超过 50KB

## 示例应用

### 最小应用模板
```html
<!DOCTYPE html>
<html lang="zh-CN">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0, user-scalable=no, viewport-fit=cover">
    <title>我的应用</title>
    <style>
        * { margin: 0; padding: 0; box-sizing: border-box; }
        body {
            font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, sans-serif;
            background: linear-gradient(135deg, #1a1a2e 0%, #16213e 100%);
            color: #ffffff;
            height: 100vh;
            display: flex;
            flex-direction: column;
        }
        .top-bar {
            display: flex;
            justify-content: space-between;
            align-items: center;
            padding: 12px 16px;
            background: rgba(0, 0, 0, 0.3);
        }
        .app-title { font-size: 16px; color: #f57309; }
        .close-btn {
            width: 32px; height: 32px;
            border-radius: 50%;
            background: rgba(255, 255, 255, 0.1);
            border: none;
            color: #fff;
            font-size: 18px;
        }
        .container {
            flex: 1;
            display: flex;
            align-items: center;
            justify-content: center;
        }
    </style>
</head>
<body>
    <div class="top-bar">
        <div class="app-title">我的应用</div>
        <button class="close-btn" onclick="closeWindow()">✕</button>
    </div>
    <div class="container">
        <h1>Hello NeoPod!</h1>
    </div>
    <script>
        function closeWindow() {
            try {
                if (typeof NeoPod !== 'undefined' && NeoPod.close) {
                    NeoPod.close();
                } else {
                    window.close();
                }
            } catch (error) {
                console.log('无法关闭窗口:', error);
            }
        }
    </script>
</body>
</html>
```

## 调试技巧

### 控制台日志
在应用中使用 console.log 输出调试信息：

```javascript
console.log('调试信息:', variable);
```

### 常见问题

1. **应用无法关闭**
   - 确保正确实现了 `NeoPod.close()` 调用
   - 检查关闭按钮是否正确绑定事件

2. **文件操作失败**
   - 确认文件路径正确
   - 检查是否有访问权限
   - 使用 try-catch 捕获错误

3. **布局显示异常**
   - 检查 viewport 设置
   - 确认使用了正确的 CSS 单位
   - 测试不同屏幕尺寸

## 最佳实践

1. **性能优化**
   - 减少 DOM 操作
   - 使用 CSS 动画代替 JavaScript 动画
   - 压缩图片和资源文件

2. **用户体验**
   - 提供加载状态反馈
   - 处理网络错误情况
   - 保存用户设置和进度

3. **安全性**
   - 不在代码中硬编码敏感信息
   - 验证用户输入
   - 使用 HTTPS 加载外部资源