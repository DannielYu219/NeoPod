# NeoPod 第三方应用开发者指南

## 概述

NeoPod 支持通过 HTML/CSS/JavaScript 开发第三方插件应用。这些应用可以在 NeoPod 的 Dev 页面中运行，提供丰富的交互功能。

## 文件结构

### 应用存放位置
- HTML 插件应用应放置在应用的 `Documents/dev/` 文件夹中
- 支持的文件格式：`.html` 和 `.htm`
- 应用会自动按文件名排序显示在 Dev 页面列表中

### 文件命名规范
- 使用英文或拼音命名，避免使用特殊字符
- 文件名应简洁明了，能够描述应用功能
- 例如：`calculator.html`、`todo_list.html`、`test_plugin.html`

## 开发规范

### HTML 文件结构要求

每个 HTML 文件开头必须包含以下注释：

```html
<!--
文件作用：[描述此文件的主要功能]
依赖：[列出所需的外部依赖，如无则写"无"]
输入：[描述用户输入方式，如按钮点击、文本输入等]
输出：[描述应用输出内容，如数据显示、界面更新等]
实现方式：[简要说明核心实现逻辑]
-->
```

**示例：**
```html
<!--
文件作用：计数器应用，支持增加、减少和重置计数
依赖：无（纯原生JavaScript）
输入：用户点击按钮进行操作
输出：实时显示当前计数值
实现方式：使用JavaScript变量存储计数值，通过DOM操作更新显示
-->
```

### 基本HTML模板

```html
<!DOCTYPE html>
<html lang="zh-CN">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0, user-scalable=no, viewport-fit=cover">
    <title>应用标题</title>
    <style>
        /* CSS样式 */
    </style>
</head>
<body>
    <!-- HTML内容 -->
    <script>
        // JavaScript代码
    </script>
</body>
</html>
```

### iOS WebView 安全区域适配

为了确保应用在 iOS 设备上正确显示，需要处理安全区域（Safe Area）：

```html
<meta name="viewport" content="width=device-width, initial-scale=1.0, user-scalable=no, viewport-fit=cover">
```

```css
:root {
    --safe-area-inset-top: env(safe-area-inset-top, 0px);
    --safe-area-inset-bottom: env(safe-area-inset-bottom, 0px);
    --safe-area-inset-left: env(safe-area-inset-left, 0px);
    --safe-area-inset-right: env(safe-area-inset-right, 0px);
}

body {
    height: 100vh;
    overflow: hidden;
    display: flex;
    flex-direction: column;
}
```

## WebView 兼容性

### 支持的技术栈
- **HTML5**：完整的 HTML5 支持
- **CSS3**：包括 Flexbox、Grid、动画等现代特性
- **JavaScript**：ES6+ 语法支持
- **API**：
  - LocalStorage（本地存储）
  - SessionStorage
  - Canvas API
  - Web Audio API
  - 等标准 Web API

### 不支持的功能
- 文件系统访问（出于安全考虑）
- 网络请求（需特殊配置）
- 原生插件
- WebGL（部分支持）

## 设计规范

### Metro 设计语言

NeoPod 采用类似 Windows Phone 7 和 Zune HD 的 Metro 设计语言，强调内容优先、动态交互和流畅动画。

#### 核心设计原则

1. **内容优先**
   - 减少装饰元素，突出核心内容
   - 使用大字体和清晰的排版
   - 信息层次分明，易于浏览

2. **动态交互**
   - 使用动画和过渡效果增强用户体验
   - 元素状态变化要有明确的视觉反馈
   - 支持触摸手势和流畅的页面切换

3. **扁平化设计**
   - 避免过度装饰和阴影
   - 使用纯色块和渐变
   - 强调功能而非视觉特效

4. **网格布局**
   - 采用对齐的网格系统
   - 元素间距统一
   - 视觉节奏一致

#### 视觉风格
NeoPod 采用深色主题设计，建议应用遵循以下设计规范：

##### 配色方案
- **主色调**：`#f57309`（橙色）
- **背景色**：`#1a1a2e` 到 `#16213e` 渐变
- **文字色**：`#ffffff`（白色）
- **次要文字色**：`rgba(255, 255, 255, 0.6)`
- **卡片背景**：`rgba(255, 255, 255, 0.05)`
- **导航栏背景**：`rgba(0, 0, 0, 0.3)`

##### 字体规范
```css
font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, sans-serif;
```

**字体大小参考：**
- 标题：18-20px（中等粗细）
- 大标题：32-72px（细体）
- 正文：14-16px
- 标签：11-12px（大写，字间距 0.5-1px）

##### 组件样式

**卡片**
- 圆角：16px
- 背景：半透明 `rgba(255, 255, 255, 0.05)`
- 模糊效果：`backdrop-filter: blur(10px)`
- 边框：`1px solid rgba(255, 255, 255, 0.1)`
- 间距：底部 16px

**按钮**
- 圆角：8-12px
- 背景：主色调
- 内边距：16px 40px
- 字体：18px，中等粗细
- 悬停效果：缩放 1.05 倍
- 点击效果：缩放 0.95 倍

**输入框**
- 圆角：8-12px
- 背景：半透明 `rgba(255, 255, 255, 0.05)`
- 边框：`1px solid rgba(255, 255, 255, 0.1)`
- 内边距：16px 20px
- 聚焦时：边框颜色变为主色调

**导航栏**
- 顶部栏：高度自适应，背景半透明
- 底部导航：固定在底部，水平排列
- 导航项：图标 + 文字，垂直排列
- 激活状态：主色调高亮

##### 图标使用
- 优先使用 Emoji 图标（简单、直观）
- 图标大小：24px（导航）、48px（空状态）
- 保持风格统一

##### 布局结构
```css
/* 推荐的应用布局结构 */
body {
    height: 100vh;
    overflow: hidden;
    display: flex;
    flex-direction: column;
}

.app-container {
    flex: 1;
    display: flex;
    flex-direction: column;
}

.top-bar {
    /* 顶部导航栏 */
}

.content-area {
    flex: 1;
    overflow: hidden;
    position: relative;
}

.bottom-nav {
    /* 底部导航栏 */
}
```

### 响应式设计
应用应适配不同屏幕尺寸：
- 使用相对单位（rem, em, %）
- 避免固定宽高
- 使用 Flexbox 或 Grid 布局
- 触摸区域至少 44x44px
- 支持横竖屏切换

## 交互设计

### Metro 风格交互

#### 页面切换
- 使用底部导航切换主要功能模块
- 页面切换使用滑动动画（translateX）
- 保持当前页面状态，避免重复加载

#### 触摸反馈
```css
.btn:active {
    transform: scale(0.95);
}

.card:hover {
    transform: translateY(-2px);
}

.nav-item:active {
    transform: scale(0.95);
}
```

#### 动画效果
```css
transition: all 0.3s ease;
```

#### 动态效果
- 计数器数值变化时使用脉冲动画
- 页面切换使用平滑过渡
- 列表项删除使用淡出效果

### 加载状态
提供清晰的加载反馈，避免用户等待时产生困惑。

## 性能优化

### 资源管理
- 避免使用大型图片，使用 SVG 或 CSS 绘图
- 压缩 CSS 和 JavaScript
- 使用事件委托减少事件监听器数量

### 内存管理
- 及时清理不再使用的 DOM 元素
- 避免内存泄漏（如未移除的事件监听器）
- 使用防抖和节流优化高频事件

### 渲染优化
- 使用 CSS transform 和 opacity 进行动画
- 避免频繁的 DOM 操作
- 使用文档片段批量更新

## 调试技巧

### 日志输出
```javascript
console.log('调试信息');
console.error('错误信息');
```

### 错误处理
```javascript
try {
    // 可能出错的代码
} catch (error) {
    console.error('发生错误:', error);
}
```

### WebView 调试
- 使用 Safari Web Inspector 连接设备调试
- 查看控制台日志
- 检查网络请求

## 示例应用

### 示例1：简单计数器
```html
<!--
文件作用：简单计数器
依赖：无
输入：用户点击按钮
输出：显示当前计数值
实现方式：使用JavaScript变量存储计数值
-->
<!DOCTYPE html>
<html lang="zh-CN">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0, user-scalable=no, viewport-fit=cover">
    <title>计数器</title>
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, sans-serif;
            background: linear-gradient(135deg, #1a1a2e 0%, #16213e 100%);
            color: #ffffff;
            height: 100vh;
            overflow: hidden;
            display: flex;
            flex-direction: column;
        }

        .app-container {
            flex: 1;
            display: flex;
            flex-direction: column;
            justify-content: center;
            align-items: center;
        }

        .counter {
            text-align: center;
        }

        .count {
            font-size: 64px;
            font-weight: 200;
            color: #ffffff;
            margin: 20px 0;
        }

        .counter-buttons {
            display: flex;
            gap: 16px;
            justify-content: center;
        }

        .btn {
            padding: 16px 40px;
            border: none;
            border-radius: 12px;
            font-size: 18px;
            font-weight: 500;
            cursor: pointer;
            transition: all 0.2s ease;
            background: #f57309;
            color: #ffffff;
        }

        .btn:hover {
            background: #ff8c3b;
            transform: scale(1.05);
        }

        .btn:active {
            transform: scale(0.95);
        }

        .btn-secondary {
            background: rgba(255, 255, 255, 0.1);
        }

        .btn-secondary:hover {
            background: rgba(255, 255, 255, 0.2);
        }
    </style>
</head>
<body>
    <div class="app-container">
        <div class="counter">
            <div class="count" id="count">0</div>
            <div class="counter-buttons">
                <button class="btn btn-secondary" onclick="decrement()">-1</button>
                <button class="btn" onclick="increment()">+1</button>
                <button class="btn btn-secondary" onclick="reset()">重置</button>
            </div>
        </div>
    </div>
    <script>
        let count = 0;
        
        function increment() {
            count++;
            updateCount();
        }
        
        function decrement() {
            count--;
            updateCount();
        }
        
        function reset() {
            count = 0;
            updateCount();
        }
        
        function updateCount() {
            const countEl = document.getElementById('count');
            countEl.textContent = count;
            countEl.style.transform = 'scale(1.1)';
            setTimeout(() => {
                countEl.style.transform = 'scale(1)';
            }, 150);
        }
    </script>
</body>
</html>
```

### 示例2：待办事项列表
```html
<!--
文件作用：待办事项管理
依赖：LocalStorage
输入：用户输入文本和点击操作
输出：显示待办事项列表
实现方式：使用LocalStorage持久化数据
-->
<!DOCTYPE html>
<html lang="zh-CN">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0, user-scalable=no, viewport-fit=cover">
    <title>待办事项</title>
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        :root {
            --safe-area-inset-top: env(safe-area-inset-top, 0px);
            --safe-area-inset-bottom: env(safe-area-inset-bottom, 0px);
        }

        body {
            font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, sans-serif;
            background: linear-gradient(135deg, #1a1a2e 0%, #16213e 100%);
            color: #ffffff;
            height: 100vh;
            overflow: hidden;
            display: flex;
            flex-direction: column;
        }

        .app-container {
            flex: 1;
            display: flex;
            flex-direction: column;
            overflow: hidden;
        }

        .top-bar {
            display: flex;
            justify-content: space-between;
            align-items: center;
            padding: 16px 20px;
            background: rgba(0, 0, 0, 0.3);
            backdrop-filter: blur(10px);
            border-bottom: 1px solid rgba(255, 255, 255, 0.1);
        }

        .app-title {
            font-size: 18px;
            font-weight: 500;
            color: #f57309;
        }

        .content-area {
            flex: 1;
            overflow-y: auto;
            padding: 20px;
        }

        .todo-input {
            width: 100%;
            padding: 16px 20px;
            background: rgba(255, 255, 255, 0.05);
            border: 1px solid rgba(255, 255, 255, 0.1);
            border-radius: 12px;
            color: #ffffff;
            font-size: 16px;
            margin-bottom: 16px;
            outline: none;
            transition: all 0.2s ease;
        }

        .todo-input:focus {
            border-color: #f57309;
            background: rgba(255, 255, 255, 0.08);
        }

        .todo-list {
            list-style: none;
        }

        .todo-item {
            display: flex;
            align-items: center;
            gap: 12px;
            padding: 14px;
            background: rgba(255, 255, 255, 0.03);
            border-radius: 10px;
            margin-bottom: 10px;
            transition: all 0.2s ease;
        }

        .todo-item:hover {
            background: rgba(255, 255, 255, 0.06);
        }

        .todo-checkbox {
            width: 24px;
            height: 24px;
            border: 2px solid rgba(255, 255, 255, 0.3);
            border-radius: 6px;
            cursor: pointer;
            display: flex;
            align-items: center;
            justify-content: center;
            transition: all 0.2s ease;
        }

        .todo-checkbox.checked {
            background: #f57309;
            border-color: #f57309;
        }

        .todo-checkbox.checked::after {
            content: '✓';
            color: #ffffff;
            font-size: 14px;
        }

        .todo-text {
            flex: 1;
            font-size: 15px;
            transition: all 0.2s ease;
        }

        .todo-text.completed {
            text-decoration: line-through;
            opacity: 0.5;
        }

        .todo-delete {
            color: #ff4757;
            cursor: pointer;
            font-size: 20px;
            opacity: 0;
            transition: all 0.2s ease;
            padding: 4px;
        }

        .todo-item:hover .todo-delete {
            opacity: 1;
        }
    </style>
</head>
<body>
    <div class="app-container">
        <div class="top-bar">
            <div class="app-title">待办事项</div>
        </div>
        <div class="content-area">
            <input type="text" class="todo-input" id="todoInput" placeholder="添加新的待办事项...">
            <ul class="todo-list" id="todoList"></ul>
        </div>
    </div>
    <script>
        let todos = JSON.parse(localStorage.getItem('todos') || '[]');
        
        function render() {
            const list = document.getElementById('todoList');
            list.innerHTML = todos.map((todo, i) => `
                <li class="todo-item">
                    <div class="todo-checkbox ${todo.done ? 'checked' : ''}" onclick="toggle(${i})"></div>
                    <span class="todo-text ${todo.done ? 'completed' : ''}">${todo.text}</span>
                    <span class="todo-delete" onclick="remove(${i})">×</span>
                </li>
            `).join('');
        }
        
        function add() {
            const input = document.getElementById('todoInput');
            const text = input.value.trim();
            if (text) {
                todos.push({ text: text, done: false });
                input.value = '';
                save();
            }
        }
        
        function toggle(i) {
            todos[i].done = !todos[i].done;
            save();
        }
        
        function remove(i) {
            todos.splice(i, 1);
            save();
        }
        
        function save() {
            localStorage.setItem('todos', JSON.stringify(todos));
            render();
        }
        
        document.getElementById('todoInput').addEventListener('keypress', e => {
            if (e.key === 'Enter') add();
        });
        
        render();
    </script>
</body>
</html>
```

## 最佳实践

### 1. 代码组织
- 将 CSS、HTML 和 JavaScript 分离到不同区域
- 使用有意义的变量和函数名
- 添加必要的注释

### 2. 用户体验
- 提供清晰的视觉反馈
- 避免使用 alert()，使用自定义提示
- 支持键盘操作（如 Enter 提交）
- 遵循 Metro 设计语言：内容优先、动态交互

### 3. 错误处理
- 验证用户输入
- 提供友好的错误提示
- 记录错误日志便于调试

### 4. 性能考虑
- 避免频繁的 DOM 操作
- 使用文档片段批量更新
- 合理使用缓存
- 使用 CSS transform 进行动画

### 5. Metro 风格实现
- 使用底部导航切换功能模块
- 页面切换使用滑动动画
- 元素状态变化有明确反馈
- 保持视觉节奏一致

## 测试建议

### 测试清单
- [ ] 在不同屏幕尺寸下测试
- [ ] 测试触摸交互
- [ ] 验证数据持久化
- [ ] 检查内存使用
- [ ] 测试边界情况
- [ ] 测试横竖屏切换
- [ ] 验证安全区域适配

### 调试工具
- 使用 Safari Web Inspector 连接设备调试
- 查看控制台日志
- 监控网络请求
- 检查动画性能

## 发布流程

### 1. 准备应用
- 确保应用功能完整
- 添加完整的文件头注释
- 测试所有功能
- 验证 Metro 设计规范

### 2. 部署
- 将 HTML 文件放入 `Documents/dev/` 文件夹
- 在 NeoPod 的 Dev 页面刷新列表
- 测试应用运行

### 3. 更新
- 直接替换文件即可更新
- 无需重新安装应用

## 常见问题

### Q: 如何访问本地存储？
A: 使用 `localStorage` API，数据会持久化保存。

### Q: 支持网络请求吗？
A: 默认不支持，如需网络功能请联系 NeoPod 团队。

### Q: 如何与其他应用交互？
A: 目前应用间相互独立，未来版本可能支持应用间通信。

### Q: 文件大小有限制吗？
A: 建议单个 HTML 文件不超过 1MB。

### Q: 支持哪些 JavaScript 框架？
A: 可以使用 Vue.js、React 等框架，但建议使用原生 JavaScript 以获得最佳性能。

### Q: 如何实现 Metro 风格的页面切换？
A: 使用底部导航 + CSS transform 实现滑动切换效果，参考测试插件示例。

### Q: 如何适配 iOS 安全区域？
A: 使用 `viewport-fit=cover` 和 CSS 环境变量 `env(safe-area-inset-*)` 进行适配。

## 技术支持

如有问题或建议，请联系 NeoPod 开发团队。

---

**版本**: 2.0  
**更新日期**: 2024  
**适用版本**: NeoPod 1.0+
