# NeoPod 第三方应用开发者指南

## 概述

NeoPod 支持通过 HTML/CSS/JavaScript 开发第三方插件应用。这些应用可以在 NeoPod 的 Dev 页面中运行，提供丰富的交互功能。

## 文件结构

### 应用存放位置
- HTML 插件应用应放置在应用的 `Documents/dev/` 文件夹中
- 支持的文件格式：`.html` 和 `.htm`
- 应用会自动按文件名排序显示在 Dev 页面列表中

### 文件命名规范
- 使用英文或拼音命名，避免使用特殊字符
- 文件名应简洁明了，能够描述应用功能
- 例如：`calculator.html`、`todo_list.html`、`test_plugin.html`

## 开发规范

### HTML 文件结构要求

每个 HTML 文件开头必须包含以下注释：

```html
<!--
文件作用：[描述此文件的主要功能]
依赖：[列出所需的外部依赖，如无则写"无"]
输入：[描述用户输入方式，如按钮点击、文本输入等]
输出：[描述应用输出内容，如数据显示、界面更新等]
实现方式：[简要说明核心实现逻辑]
-->
```

**示例：**
```html
<!--
文件作用：计数器应用，支持增加、减少和重置计数
依赖：无（纯原生JavaScript）
输入：用户点击按钮进行操作
输出：实时显示当前计数值
实现方式：使用JavaScript变量存储计数值，通过DOM操作更新显示
-->
```

### 基本HTML模板

```html
<!DOCTYPE html>
<html lang="zh-CN">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0, user-scalable=no">
    <title>应用标题</title>
    <style>
        /* CSS样式 */
    </style>
</head>
<body>
    <!-- HTML内容 -->
    <script>
        // JavaScript代码
    </script>
</body>
</html>
```

## WebView 兼容性

### 支持的技术栈
- **HTML5**：完整的 HTML5 支持
- **CSS3**：包括 Flexbox、Grid、动画等现代特性
- **JavaScript**：ES6+ 语法支持
- **API**：
  - LocalStorage（本地存储）
  - SessionStorage
  - Canvas API
  - Web Audio API
  - 等标准 Web API

### 不支持的功能
- 文件系统访问（出于安全考虑）
- 网络请求（需特殊配置）
- 原生插件
- WebGL（部分支持）

## 设计规范

### 视觉风格
NeoPod 采用深色主题设计，建议应用遵循以下设计规范：

#### 配色方案
- **主色调**：`#f57309`（橙色）
- **背景色**：`#1a1a2e` 到 `#16213e` 渐变
- **文字色**：`#ffffff`（白色）
- **次要文字色**：`rgba(255, 255, 255, 0.6)`
- **卡片背景**：`rgba(255, 255, 255, 0.05)`

#### 字体规范
```css
font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, sans-serif;
```

#### 组件样式
- **卡片**：圆角 16px，半透明背景，模糊效果
- **按钮**：圆角 8px，主色调背景，悬停效果
- **输入框**：圆角 8px，半透明背景，聚焦时边框高亮

### 响应式设计
应用应适配不同屏幕尺寸：
- 使用相对单位（rem, em, %）
- 避免固定宽高
- 使用 Flexbox 或 Grid 布局
- 触摸区域至少 44x44px

## 交互设计

### 触摸反馈
```css
.btn:active {
    transform: scale(0.95);
}

.card:hover {
    transform: translateY(-2px);
}
```

### 动画效果
```css
transition: all 0.3s ease;
```

### 加载状态
提供清晰的加载反馈，避免用户等待时产生困惑。

## 性能优化

### 资源管理
- 避免使用大型图片，使用 SVG 或 CSS 绘图
- 压缩 CSS 和 JavaScript
- 使用事件委托减少事件监听器数量

### 内存管理
- 及时清理不再使用的 DOM 元素
- 避免内存泄漏（如未移除的事件监听器）
- 使用防抖和节流优化高频事件

## 调试技巧

### 日志输出
```javascript
console.log('调试信息');
console.error('错误信息');
```

### 错误处理
```javascript
try {
    // 可能出错的代码
} catch (error) {
    console.error('发生错误:', error);
}
```

## 示例应用

### 示例1：简单计数器
```html
<!--
文件作用：简单计数器
依赖：无
输入：用户点击按钮
输出：显示当前计数值
实现方式：使用JavaScript变量存储计数值
-->
<!DOCTYPE html>
<html lang="zh-CN">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>计数器</title>
    <style>
        body {
            background: #1a1a2e;
            color: #fff;
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
            margin: 0;
        }
        .counter {
            text-align: center;
        }
        .count {
            font-size: 64px;
            color: #f57309;
            margin: 20px 0;
        }
        .btn {
            padding: 12px 24px;
            background: #f57309;
            border: none;
            border-radius: 8px;
            color: #fff;
            font-size: 16px;
            margin: 0 8px;
            cursor: pointer;
        }
    </style>
</head>
<body>
    <div class="counter">
        <div class="count" id="count">0</div>
        <button class="btn" onclick="increment()">+1</button>
        <button class="btn" onclick="decrement()">-1</button>
    </div>
    <script>
        let count = 0;
        function increment() {
            count++;
            document.getElementById('count').textContent = count;
        }
        function decrement() {
            count--;
            document.getElementById('count').textContent = count;
        }
    </script>
</body>
</html>
```

### 示例2：待办事项列表
```html
<!--
文件作用：待办事项管理
依赖：LocalStorage
输入：用户输入文本和点击操作
输出：显示待办事项列表
实现方式：使用LocalStorage持久化数据
-->
<!DOCTYPE html>
<html lang="zh-CN">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>待办事项</title>
    <style>
        body {
            background: #1a1a2e;
            color: #fff;
            padding: 20px;
            font-family: -apple-system, sans-serif;
        }
        .input {
            width: 100%;
            padding: 12px;
            background: rgba(255,255,255,0.1);
            border: none;
            border-radius: 8px;
            color: #fff;
            margin-bottom: 20px;
        }
        .todo-item {
            background: rgba(255,255,255,0.05);
            padding: 12px;
            margin-bottom: 8px;
            border-radius: 8px;
            display: flex;
            align-items: center;
        }
        .todo-item.completed {
            opacity: 0.5;
            text-decoration: line-through;
        }
        .checkbox {
            width: 20px;
            height: 20px;
            border: 2px solid #f57309;
            border-radius: 4px;
            margin-right: 12px;
            cursor: pointer;
        }
        .checkbox.checked {
            background: #f57309;
        }
        .delete {
            margin-left: auto;
            color: #ff4757;
            cursor: pointer;
        }
    </style>
</head>
<body>
    <input type="text" class="input" id="input" placeholder="添加待办事项...">
    <div id="list"></div>
    <script>
        let todos = JSON.parse(localStorage.getItem('todos') || '[]');
        
        function render() {
            const list = document.getElementById('list');
            list.innerHTML = todos.map((todo, i) => `
                <div class="todo-item ${todo.done ? 'completed' : ''}">
                    <div class="checkbox ${todo.done ? 'checked' : ''}" onclick="toggle(${i})"></div>
                    <span>${todo.text}</span>
                    <span class="delete" onclick="remove(${i})">×</span>
                </div>
            `).join('');
        }
        
        function add() {
            const input = document.getElementById('input');
            if (input.value.trim()) {
                todos.push({ text: input.value, done: false });
                input.value = '';
                save();
            }
        }
        
        function toggle(i) {
            todos[i].done = !todos[i].done;
            save();
        }
        
        function remove(i) {
            todos.splice(i, 1);
            save();
        }
        
        function save() {
            localStorage.setItem('todos', JSON.stringify(todos));
            render();
        }
        
        document.getElementById('input').addEventListener('keypress', e => {
            if (e.key === 'Enter') add();
        });
        
        render();
    </script>
</body>
</html>
```

## 最佳实践

### 1. 代码组织
- 将 CSS、HTML 和 JavaScript 分离到不同区域
- 使用有意义的变量和函数名
- 添加必要的注释

### 2. 用户体验
- 提供清晰的视觉反馈
- 避免使用 alert()，使用自定义提示
- 支持键盘操作（如 Enter 提交）

### 3. 错误处理
- 验证用户输入
- 提供友好的错误提示
- 记录错误日志便于调试

### 4. 性能考虑
- 避免频繁的 DOM 操作
- 使用文档片段批量更新
- 合理使用缓存

## 测试建议

### 测试清单
- [ ] 在不同屏幕尺寸下测试
- [ ] 测试触摸交互
- [ ] 验证数据持久化
- [ ] 检查内存使用
- [ ] 测试边界情况

### 调试工具
- 使用 Safari Web Inspector 连接设备调试
- 查看控制台日志
- 监控网络请求

## 发布流程

### 1. 准备应用
- 确保应用功能完整
- 添加完整的文件头注释
- 测试所有功能

### 2. 部署
- 将 HTML 文件放入 `Documents/dev/` 文件夹
- 在 NeoPod 的 Dev 页面刷新列表
- 测试应用运行

### 3. 更新
- 直接替换文件即可更新
- 无需重新安装应用

## 常见问题

### Q: 如何访问本地存储？
A: 使用 `localStorage` API，数据会持久化保存。

### Q: 支持网络请求吗？
A: 默认不支持，如需网络功能请联系 NeoPod 团队。

### Q: 如何与其他应用交互？
A: 目前应用间相互独立，未来版本可能支持应用间通信。

### Q: 文件大小有限制吗？
A: 建议单个 HTML 文件不超过 1MB。

### Q: 支持哪些 JavaScript 框架？
A: 可以使用 Vue.js、React 等框架，但建议使用原生 JavaScript 以获得最佳性能。

## 技术支持

如有问题或建议，请联系 NeoPod 开发团队。

---

**版本**: 1.0  
**更新日期**: 2024  
**适用版本**: NeoPod 1.0+
